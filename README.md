student_name = input("Enter students name: ")
score = [25,30,45]
average = sum(scores) / len score
print(average)
 
file_name = input("Enter file name:")
if opened requarements needed
try(no file found)
exept("no file found")
